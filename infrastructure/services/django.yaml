Description: OMBU Site / services / django

Parameters:

  AppVersion:
    Description: The application version
    Type: String

  Cluster:
    Description: The container cluster that runs the services
    Type: String

  DatabaseHost:
    Description: The database hostname
    Type: String

  DatabaseName:
    Description: The database name
    Type: String

  DatabasePassword:
    Description: The database user password
    Type: AWS::SSM::Parameter::Value<String>
    NoEcho: True

  DatabaseUser:
    Description: The database user
    Type: String

  DesiredCount:
    Type: Number
    Default: 2

  Domain:
    Type: String
    Description: The domain for the webite

  EnvironmentType:
    Description: The application environment.
    Type: String

  Listener:
    Description: The load balancer listener for the web service
    Type: String

  Priority:
    Description: The ELB priority order for this service
    Type: Number

  RoleArn:
    Description: The ARN of an IAM role for the service
    Type: String

  VPC:
    Description: The VPC for the cluster
    Type: AWS::EC2::VPC::Id

Resources:

  CloudWatchLogsGroup:
    Type: AWS::Logs::LogGroup
    Properties:
      LogGroupName: !Ref AWS::StackName
      RetentionInDays: 60

  TargetGroup:
    Type: AWS::ElasticLoadBalancingV2::TargetGroup
    Properties:
      VpcId: !Ref VPC
      Port: 80
      Protocol: HTTP
      HealthCheckPath: /health-check
      HealthyThresholdCount: 2
      Matcher:
        # Allowing 301 because often the default target hosts
        # the redirector service
        HttpCode: 200-301
      TargetGroupAttributes:
        - Key: deregistration_delay.timeout_seconds
          Value: 5
      UnhealthyThresholdCount: 2

  TaskDefinition:
    Type: AWS::ECS::TaskDefinition
    Properties:
      Family: !Sub ombusite_${EnvironmentType}_django
      ContainerDefinitions:
        - Name: django
          Essential: true
          Image: !Sub ${AWS::AccountId}.dkr.ecr.${AWS::Region}.amazonaws.com/ombusite/django:${AppVersion}
          MemoryReservation: 128
          LogConfiguration:
            LogDriver: awslogs
            Options:
              awslogs-group: !Ref CloudWatchLogsGroup
              awslogs-region: !Ref AWS::Region
          PortMappings:
            - ContainerPort: 5000
          Environment:
            - Name: APPLICATION_VERSION
              Value: !Ref AppVersion
            - Name: DATABASE_NAME
              Value: ombusite
            - Name: DATABASE_ENDPOINT
              Value: !Ref DatabaseHost
            - Name: DATABASE_USER
              Value: ombusite
            - Name: DOMAIN
              Value: !Ref Domain
            - Name: ENVIRONMENT_TYPE
              Value: !Ref EnvironmentType
            - Name: DATABASE_PASSWORD
              Value: !Ref DatabasePassword

  ListenerRule:
    Type: AWS::ElasticLoadBalancingV2::ListenerRule
    Properties:
      ListenerArn: !Ref Listener
      Priority: !Ref Priority
      Conditions:
        - Field: host-header
          Values:
            - !Ref Domain
      Actions:
        - TargetGroupArn: !Ref TargetGroup
          Type: forward

  Service:
    Type: AWS::ECS::Service
    DependsOn: ListenerRule
    Properties:
      Cluster: !Ref Cluster
      Role: !Ref RoleArn
      DesiredCount: !Ref DesiredCount
      TaskDefinition: !Ref TaskDefinition
      LoadBalancers:
        - ContainerName: django
          ContainerPort: 5000
          TargetGroupArn: !Ref TargetGroup
      DeploymentConfiguration:
        MaximumPercent: 200
        MinimumHealthyPercent: 50


Outputs:

  ServiceArn:
    Description: The ARN the service created by this stack
    Value: !Ref Service

  ServiceName:
    Description: The name the service created by this stack
    Value: !GetAtt Service.Name
